<% if (item.categories && item.categories.length){ %>
  <%
  var cats = [];
  item.categories.forEach(function(cat){
    cats.push('<a href="' + config.root + cat.path + '">' + cat.name + '</a>');
  });
  %>
  <span id="post-category" class="author-item">分类：「<a href="#"><%- cats.join(', ') %></a>」</span>
<% } %>